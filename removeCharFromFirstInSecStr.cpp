#include<iostream>
using namespace std;
string solve(string str1,string str2)
{
    string ans;
    for(int i=0;i<str1.length();i++)
    {
        int flag=0;
        for(int j=0;j<str2.length();j++)
        {
            if(str1[i]==str2[j])
            {
                flag=1;
            }
        }
        if(flag=0;)
        {
            if(int i=0;i<)
        }
    }
}